{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "if [ ! -d /usr/lib/flutter ]; then git clone https://github.com/flutter/flutter.git -b stable /usr/lib/flutter; fi && /usr/lib/flutter/bin/flutter pub get",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
